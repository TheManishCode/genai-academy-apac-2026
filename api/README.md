# API Layer

The runnable FastAPI implementation lives in `backend/app/api`.

This top-level folder is reserved for API contracts, OpenAPI exports, Postman collections and partner-facing integration artifacts.
